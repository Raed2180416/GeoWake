import 'package:flutter_test/flutter_test.dart';

// Deprecated: Progress bar widget removed in favor of notification-only progress.
// Keeping a placeholder test so this file is inert and passes independently.
void main() {
	test('route_progress_bar_removed_placeholder', () {
		expect(true, isTrue);
	});
}
